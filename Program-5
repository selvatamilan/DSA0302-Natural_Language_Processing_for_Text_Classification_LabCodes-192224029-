import nltk
from nltk.stem import PorterStemmer


stemmer = PorterStemmer()

words = ["running", "flies", "easily", "fairly", "happiness", "studies", "cats", "meeting"]

print("Original → Stemmed")
for word in words:
    print(f"{word} → {stemmer.stem(word)}")
